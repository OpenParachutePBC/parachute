# Parachute Docker Configuration

# Path to your vault (Obsidian vault or Parachute folder)
# This will be mounted into the container at /vault
# Examples:
#   macOS/Linux: /Users/yourname/Obsidian/Parachute
#   macOS/Linux: ~/Parachute
VAULT_PATH=~/Parachute

# Optional: Anthropic API Key
# If not set, the backend will try to use Claude OAuth credentials
# from ~/.claude/.credentials.json (must be mounted in docker-compose.yml)
#ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Server Configuration (usually don't need to change these)
#SERVER_PORT=8080
#SERVER_HOST=0.0.0.0

# Database path (inside container, persisted in volume)
#DB_PATH=/app/data/parachute.db
